<%= form_with(model: resume_datum) do |form| %>
  <% if resume_datum.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(resume_datum.errors.count, "error") %> prohibited this resume_data from being saved:</h2>

      <ul>
        <% resume_datum.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :files, style: "display: block" %>
    <%= form.file_field :files, multiple: true %>
  </div>

  <div>
    <%= form.submit %>
  </div>
  <script>
    const app= new Vue({
      data:()=>({files:null}),
    
    })
    </script>
<% end %>
